# 安全策略

[English](SECURITY.md) | 简体中文

## 支持版本

安全修复面向最新发布的 `0.x` 版本。报告问题前应先确认该问题是否已经在 `main` 或后续版本中
修复，并优先升级到最新 Patch 版本。

## 报告安全问题

不得在公开 Issue 中披露凭据、个人信息、生产数据、漏洞利用细节或可直接复现攻击的材料。请使用
[GitHub 私有安全公告](https://github.com/kongbu0621/watch-engine/security/advisories/new) 报告。
如果私有渠道不可用，只创建不含技术细节的最小公开 Issue，请求维护者提供私下联系渠道。

报告应包含受影响版本、影响范围、触发条件以及已经脱敏的最小复现。该项目是社区项目，维护者会在
能力允许时确认和处理，但不承诺固定响应时间 SLA。

## 部署安全基线

- 使用专用非特权账号运行，并将状态目录设为仅所有者可访问（`0700`）；
- 不要把 SQLite 数据库放在共享或不可信文件系统；
- 数据库、WAL、SHM 文件必须保持 `0600`，POSIX 下由 `SQLiteStore` 强制收紧；
- 不记录完整 Observation、Event、异常对象、环境变量或数据库行；
- 持续更新 Python、运行时依赖和宿主操作系统；
- 备份采用不低于在线数据的访问控制，并配置明确的删除周期。

`watch-engine` 是可复用程序库，不是完整安全边界。Observer、TransitionPolicy、EventSink、进程
监督、秘密管理、网络控制和下游授权仍由采用者负责。
